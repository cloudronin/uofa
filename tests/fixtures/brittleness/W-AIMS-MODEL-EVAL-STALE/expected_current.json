{
  "_doc": "v0.4 string-inequality: triggering fires correctly (versions differ AND eval is older), but ambiguous fires WRONGLY (versions differ but eval is newer — false positive). Brittleness baseline.",
  "fires_pattern": "W-AIMS-MODEL-EVAL-STALE",
  "v0_4_behavior_triggering": "fires (correct by accident)",
  "v0_4_behavior_ambiguous": "fires (WRONG — false positive)",
  "rationale": "String inequality treats any difference as staleness; doesn't distinguish older/newer."
}
