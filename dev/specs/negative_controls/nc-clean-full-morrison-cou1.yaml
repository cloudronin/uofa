spec_id: adv-2026-p2-201-nc-clean-full-morrison-cou1
description: >
  Phase 2 negative_control spec — fully-assessed Morrison COU1 archetype. Expected outcome COV-CLEAN-CORRECT
  (no rules fire). A firing on this spec is a precision bug.

target:
  weakener: null
  defeater_type: null
  uncertainty_category: structural
  coverage_intent: negative_control
  source_taxonomy: control/none

pack: vv40

package_context:
  base_cou: packs/vv40/examples/morrison/cou1
  mode: skeleton
  factors:
    - Model form
    - Output comparison
    - Relevance of the validation activities to the COU
  decision: Accepted
  mrl: 2

generation:
  model: claude-sonnet-4-6
  n_variants: 2
  subtlety: high
  # Phase 2.5 v0.5.13: bumped 8K → 10K (Complete-profile content-heavy).
  # Phase 2.5 v0.5.14: bumped 10K → 14K. Phase C holdout observed
  # 11.1% NC GEN-INVALID rate (vs 2.2% on M5) driven by token-limit
  # truncation. 14K gives ~40% headroom over typical Complete-profile
  # NC content (SA + envelope + 13 factors + offset rationale).
  max_tokens: 14000
