name: Deploy Gap-Finder to HF Space

# Deploys the Gradio Gap-Finder to the HF Docker Space cloudronin/uofa-demo.
# HF builds the image itself from the pushed source, so this workflow syncs the
# Space layout and HF rebuilds. Auth is keyless via a Trusted Publisher: the
# GitHub OIDC token is exchanged for a short-lived, repo-scoped HF token.
#
# Trusted-publisher setup (one time, on the Space):
#   spaces/cloudronin/uofa-demo -> Settings -> Trusted Publishers -> GitHub Actions
#   claims: repository = <owner>/<repo>, branch = main, workflow = deploy-space.yml

on:
  push:
    branches: [main]
    paths:
      - "space/**"
      - "src/**"
      - "packs/**"
      - "spec/**"
      - "specs/**"
      - "build-config/**"
      - "pyproject.toml"
      - "keys/research.pub"
      - ".github/workflows/deploy-space.yml"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install package + Space deps
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[extract]" gradio "huggingface_hub>=1.19.0"
      - name: Run Space tests (Jena-dependent tests skip without the jar)
        run: pytest tests/space -q

  deploy:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      id-token: write   # mint the GitHub OIDC token
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install "huggingface_hub>=1.19.0"

      - name: Exchange GitHub OIDC token for a short-lived HF token
        run: |
          set -euo pipefail
          ID_TOKEN=$(curl -sS \
            -H "Authorization: bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
            "${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=https://huggingface.co" \
            | python3 -c "import sys,json;print(json.load(sys.stdin)['value'])")
          HF_TOKEN=$(curl -sS -X POST https://huggingface.co/oauth/token \
            -H "Content-Type: application/json" \
            -d "{\"grant_type\":\"urn:ietf:params:oauth:grant-type:token-exchange\",\"subject_token_type\":\"urn:ietf:params:oauth:token-type:id_token\",\"subject_token\":\"${ID_TOKEN}\",\"resource\":\"spaces/cloudronin/uofa-demo\"}" \
            | python3 -c "import sys,json;print(json.load(sys.stdin)['access_token'])")
          echo "::add-mask::$HF_TOKEN"
          echo "HF_TOKEN=$HF_TOKEN" >> "$GITHUB_ENV"

      - name: Sync to the HF Space (HF rebuilds the image)
        env:
          HF_SPACE_REPO: cloudronin/uofa-demo
        run: python space/deploy_to_hf.py
