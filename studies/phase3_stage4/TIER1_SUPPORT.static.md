# Tier-1 restatement — support package

W10 of the Ch4 Numbers and Repairs spec. **Digs, does not write.** The R2
reclassification paragraph is the author's; this file supplies its numbers,
quotations and locations so none of them has to be dug twice.

Generated by `studies/phase3_stage4/build_tier1_support.py`; every figure traces
to a committed artifact named beside it.

---

## 1. The 12 REAL-GAP spot-check rows

Ensemble majority **REAL-GAP** on all twelve; author verdict **OUT-OF-SCOPE** on
all twelve. Rationales are the author's own, from the worksheet.

| # | case_id | ensemble | author |
|---|---|---|---|
{{ROWS}}

### The rationales, verbatim

{{RATIONALES}}
---

## 2. The author's OUT-OF-SCOPE policy, as written

From the `Cheat sheet` worksheet of
`dev/build/adversarial/phase2/2026-04-26/Stage 4 adjudication.xlsx`:

> • **OUT-OF-SCOPE**: the weakness is real but not the catalog's job (substantive
> quality, prose-only content).

> • Real weakness but substantive/prose-level, not structural → **OUT-OF-SCOPE**,
> and say why in the rationale; these are **boundary-section material**.

And the standing trap the gap-probe branch rests on:

> 1. **Prose does not count.** Rules read properties, not narrative.

The adjudication instructions
(`dev/build/adversarial/phase3/triage/ADJUDICATION_INSTRUCTIONS.md:66`) define the
verdict slightly differently — "the bundle is structurally complete but lacks the
evidence needed to evaluate the defeater" — and the author's operative reading is
the cheat-sheet one. Worth naming in the paragraph, since the two are not
identical and the twelve rulings follow the cheat sheet.

---

## 3. The original claim already deferred to Stage 4

This is the load-bearing quotation for the restatement. From
`dev/build/adversarial/phase3/STAGE3_RESULT.md:56`:

> **Stage 4 has not run.** These are ensemble candidates. The spec makes author
> adjudication the step that confirms a REAL-GAP, so the defensible present claim
> is that **6 of 6 Tier-1 candidates have majority-judge support, not that 6 of 6
> are confirmed real gaps**. Self-blinded adjudication of a sample is what
> converts the one into the other.

Stage 4 has now run, and it did not convert it. The restatement therefore
**completes a deliberately deferred claim rather than retracting an overreach** —
a materially easier paragraph to write, and a more accurate one.

The same file already carried the version-skew limit (`:58`), which R1b's
disclosure now quantifies at 63 of 65.

### Every location the 6-of-6 claim appears

Listed for the author to apply the restatement at once. **No edits made here.**

| Location | Wording |
|---|---|
| `dev/build/adversarial/phase3/STAGE3_RESULT.md:46` | "All 6 of 6 Tier-1 candidates carry majority-cited REAL-GAP support" — already correctly scoped at `:56` |
| `docs/investigations/SUMMARY.md:61` | "all 6 of 6 Tier-1 candidates supported" |
| `docs/investigations/INV-8-findings.md:161` | "**All 6 of 6 Tier-1 candidates supported**" (commit `e3d9bed8`) |
| `docs/investigations/INV-15-m5-scale-and-phase3-gap-probes.md:74` | "**6 of 6 Tier-1 supported** result would be resting on an artifact" |
| `docs/UofA_Unified_Repair_Spec_v2_1.md:179` | "all 6 Tier-1 gap-probe candidates supported" |
| `studies/phase3_stage4/README.md:22` | "289 REAL-GAP verdicts underpin the 6-of-6 Tier-1 result" |

---

## 4. Grounding, scoped to the REAL-GAP verdicts

From `check_gap_probe_grounding.py`, over 990 gap-probe judgments:

| Slice | n | median distinctive tokens | zero-echo |
|---|---:|---:|---:|
| REAL-GAP, probe points at a Tier-1 candidate | 350 | 15 | **0.0%** |
| REAL-GAP, probe points at **no** candidate | **496** | 17 | **0.0%** |

The second row is the one the paragraph needs. For those 496 the prompt header
named no candidate, so a judge had nothing to echo from it — and every one of them
still echoed package-distinctive tokens. **The ensemble was reading the packages.**
The 12-of-12 override is a disagreement about what counts as a gap, not a failure
of attention.

---

## 5. INV-20 triangulation — pointers, not prose

- **Empty rooms.** `uofa:Requirement`, `uofa:AssuranceClaim`,
  `uofa:OperatingEnvelope`, `uofa:ApplicabilityConstraint`: declared classes,
  **0** declared properties each.
- **Undeclared terms in use.** `specification`, `acceptanceThreshold`,
  `comparisonValue`, `comparisonMetric`, `quantityOfInterest`, `passed` — used in
  the corpus, declared in neither vocabulary nor context.
- **Optional structure goes unpopulated.** `requiredVerificationMethod` is
  declared and rule-read, populated in **1 of 78** packages.
- **No inference element.** `factorConstraintWarrants` is `owl:deprecated`, its
  comment reading *"not used by any package or shape"*.
- **Worked example.** Row 16, `adv-2026-p2-119-confusion-necessary-sufficient_high-v03`
  — V&V 40 compliant, every structural fact correct, and the decision record
  argues a necessary condition is sufficient. Adjudicating it required a human to
  read a paragraph and name a fallacy.

Sources: [INV-20](../../docs/investigations/INV-20-rq1-schema-adequacy.md),
[INV-21](../../docs/investigations/INV-21-claim-node-conventions.md),
[INV-19](../../docs/investigations/INV-19-requirement-layer-absent.md).

**Three routes converge on the same boundary.** The twelve rationales above reach
it case by case; INV-20 reaches it by auditing the schema; the argument-layer
prototype reaches it by building the missing layer and showing the defects become
machine-checkable once it exists
(`dev/prototypes/argument-layer/RESULTS.md`).
